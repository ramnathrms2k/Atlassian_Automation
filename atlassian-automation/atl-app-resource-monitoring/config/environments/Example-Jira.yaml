# Example Jira environment — replace server hostnames and domain with your values
environment: Example-Jira
app_type: jira
domain: .example.com

servers:
  - jira-app-01
  - jira-app-02
  - jira-app-03

paths:
  dbconfig: /export/jirahome/dbconfig.xml
  jira_home: /export/jirahome
  setenv: /export/jira/bin/setenv.sh

ssh_user: svcjira
app_port: 8080
